apiVersion: v1
kind: Service

metadata:
  name: kafka-demo-service

  labels:
    app: kafka-demo

spec:
  type: NodePort

  selector:
    app: kafka-demo

  ports:
    - name: http
      port: 8081
      targetPort: 8081
      nodePort: 30081